<% @rooms.each do |room| %>
  <div class='room'>
    <%= link_to room.title, room_path(room) %>

    <%= image_tag room.image(:large) %>
  </div>
<% end -%>


<%= link_to "Add a room", new_room_path %>
